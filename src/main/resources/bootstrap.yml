spring:  application:    name: test-ribbon-consumer  cloud:    config:      name: config-server,userservice      profile: native      discovery:        enabled: true        service-id: config-server#server:    # port: 5656 # tomcat: #   uri-encoding: UTF-8# eureka 的config信息必须填写在bootstrap.yml中，否则会出现注册不上eureka的异常信息    eureka:  client:    serviceUrl:      defaultZone: http://peer1:5959/eureka/,http://peer2:5958/eureka/#eureka.client.fetch-registry    #fetch-registry: true#eureka.client.registry-fetch-interval-seconds=8    #registry-fetch-interval-seconds: 8#eureka.instance.lease-renewal-interval-in-seconds#eureka.instance.lease-renewal-interval-in-seconds  #instance:   # lease-expiration-duration-in-seconds: 12   # lease-renewal-interval-in-seconds: 4     